#include "main.h"

/*
 * _strlen - returns length of a string
 * @s: value whose length is calculated
 *
 * Return: string length
 */
int _strlen(char *s)
{
	int c = 0;
	
	if (; *s != '/0'; s++)
	{
		c++;
	}
	return (c);
}
